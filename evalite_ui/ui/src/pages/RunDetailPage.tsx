// PLACEHOLDER — replaced in a later task
// Will fetch GET /api/v1/runs/{id} and /api/v1/runs/{id}/results, render
// case results with tag/pass-fail filters (see Task 4.1).
function RunDetailPage() {
  return (
    <div className="p-6 text-gray-900 dark:text-gray-100">
      <h1 className="text-xl font-semibold">RunDetailPage</h1>
    </div>
  );
}

export default RunDetailPage;
