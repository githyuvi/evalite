// PLACEHOLDER — replaced in a later task
// Will connect to WS /ws/v1/runs/{runId} on mount, render a LiveFeed and
// progress bar, and auto-navigate to RunDetailPage on run_completed
// (see Task 3.2).
function LiveRunPage() {
  return (
    <div className="p-6 text-gray-900 dark:text-gray-100">
      <h1 className="text-xl font-semibold">LiveRunPage</h1>
    </div>
  );
}

export default LiveRunPage;
