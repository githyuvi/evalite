// PLACEHOLDER — replaced in a later task
// Will fetch GET /api/v1/runs and render a RunsTable (see Task 3.1).
function RunsPage() {
  return (
    <div className="p-6 text-gray-900 dark:text-gray-100">
      <h1 className="text-xl font-semibold">RunsPage</h1>
    </div>
  );
}

export default RunsPage;
