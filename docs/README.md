# evalite documentation

- [Protocols](protocols.md): the three extension points (`AgentAdapter`,
  `Scorer`, `ConversationDriver`) that let you plug your own agent,
  scoring logic, or conversation behavior into evalite.
- [CLI reference](cli.md): `run`, `serve`, `db migrate`, `results`.

For install instructions and a quickstart, see the
[main README](../README.md).
